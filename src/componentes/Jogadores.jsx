import React from 'react';
// import Hmb from 'hamburguer.svg'
// eslint-disable-next-line import/no-anonymous-default-export
export default (props) => {
    return (
        <div>
        nome:  {props.nome}
        </div>
        )
    }